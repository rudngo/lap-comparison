# --- Web stack (compatible pins) ---
fastapi==0.110.3
starlette==0.37.2
pydantic==2.39.0          # maps to pydantic-core==2.39.0 (wheel available)
uvicorn[standard]==0.30.0
httpx==0.27.0
anyio==4.4.0
sniffio==1.3.1
typing-extensions==4.12.2

# --- S3 client ---
boto3==1.34.122
botocore==1.34.122

# --- Modal client SDK (gives `import modal`)
modal-client==0.62.20

# --- JSON wheel (avoid Rust build)
orjson==3.10.7

# Upload parsing
python-multipart==0.0.9
