# --- Web stack (compatible pins) ---
fastapi
starlette
pydantic
uvicorn[standard]
httpx
anyio
sniffio
typing-extensions

# --- S3 client ---
boto3
botocore

# --- Modal client SDK (gives `import modal`)
modal-client

# --- JSON wheel (avoid Rust build)
orjson

# Upload parsing
python-multipart
