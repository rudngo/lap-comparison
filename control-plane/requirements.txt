# --- Web stack (compatible pins) ---
fastapi==0.110.3
starlette==0.37.2
pydantic==2.6.4
uvicorn[standard]==0.30.0
httpx==0.26.0
anyio==4.3.0
sniffio==1.3.1
typing-extensions==4.10.0

# --- S3 client ---
boto3==1.34.122
botocore==1.34.122

# --- Modal client (SDK only; gives `import modal`) ---
modal-client==0.62.20

# --- JSON wheel (avoid Rust/Cargo build) ---
orjson==3.10.7

# Upload parsing
python-multipart==0.0.9
